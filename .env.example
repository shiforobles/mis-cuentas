# Supabase — copiá este archivo a .env y completá con tus credenciales.
# La anon key es pública por diseño (va al cliente), pero igual la mantenemos
# fuera del código. NO commitees el .env real.
#
# En GitHub Actions, cargá estas dos como Secrets del repo
# (Settings → Secrets and variables → Actions) con los mismos nombres.

VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=TU_ANON_KEY
